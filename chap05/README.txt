[참조타입]

1. 배열타입(array)
2. 열거타입(enum)
3. 클래스(class)
	- 래퍼클래스(Wrapper)
		. Byte(byte)
		. Character(char)
		. Short(short)
		. Integer(int)
		. Long(long)
		. Float(float)
		. Double(double)
		. Boolean(boolean)
	- 문자열 : String
		. 클래스형이면 기본 타입처러 사용 가능
4. 인터페이스(inteface)

5. 배열의 초기값 : 로컬변수, 멤버변수
	- 기본타입 : 정수(0), 실수(0.0), 논리형(false)
	- 클래스 : null
